# Wiki Compiler 架构：从 v2 学术级编排到 v3 三层卷积知识架构

> 本文档记录 Wiki Compiler 的架构演进。v3 在 v2 的"学术严谨性"设计基础上，引入三层卷积知识表示，形成完整的"编译流水线"。

## 演进脉络

| 版本 | 核心理念 | 关键机制 |
|------|----------|----------|
| v1 | LLM 协助整理笔记 | 手动触发、逐篇精炼 |
| v2 | 学术级知识编排引擎 | 防御性质检 + Map-Reduce 句级溯源 |
| **v3（当前）** | **三层卷积知识架构** | Raw → Refined → Summary 逐层压缩 + 增量账本 + 做梦机制 |

---

## v3 核心：三层卷积架构 (CNN-style)

借鉴 CNN 逐层缩小感受野、压缩特征的思想：

```
Layer 1 (Raw)      全量原始资料        感受野最大，信息最全     raw/
Layer 2 (Refined)  精炼 Markdown+双链   结构化压缩             wiki/{concepts,projects}
Layer 3 (Summary)  总结 + 标签索引      最小粒度，快速定位      wiki/.index/*.summary.md
```

**访问路径（逐层下钻）**：Layer 3（扫标签/总结）→ Layer 2（精炼版）→ Layer 1（全文）。
**编译路径（自底向上）**：raw 增量 → LLM 提炼 Layer 2 → 自动生成 Layer 3 索引。

---

## 保留自 v2 的核心设计

### 一、防御性质检 (Defensive Linting) 与裁判模型

在初级系统中，"自动占位 (Auto-Stub)" 往往会诱导 LLM 强行脑补缺失的概念。v2 引入的健康检查机制在 v3 中保留（`health_vis_engine.py`）：

1. **停止强行推断**：扫描到高频死链（被多次引用但未实体的概念）时，不强行生成定义。
2. **引文锚点卡片**：自动抓取相关上下文片段，打包生成带警告性质的锚点卡片。
3. **状态隔离流**：卡片打上 `maturity: stub` + `⚠️ Definition_Needed` 标记，由 wiki-dream 裁判模型（或人工）基于真实上下文做二元分类，不足以支持则建议联网外扩。

### 二、Map-Reduce 化身与句级溯源 (Wiki Weaver)

`/wiki-weaver` 命令（`wiki_weaver.py` + LLM 编排）多级萃取引擎：

- **阶段 1 - Map**：BM25 检索（`query_wiki.py`）产出高相关文件路径清单。
- **阶段 2 - 逐篇萃取**：LLM 逐篇读取，生成含 `核心论点 / 方法论 / 局限性` 的微型记忆元。
- **阶段 3 - Reduce**：聚类分析，寻找"学术共识"与"意见分歧"边界。
- **阶段 4 - Synthesis（强制句级溯源）**：任何一行推断与陈述，句末必须带 `[[原始文献]]` 溯源标志；查无源头整句删除。

### 三、幂等增量账本

`compiled_ledger.json`（`.index/`）维护 raw 文件指纹（哈希），已处理文件绝不二次重写——保证大规模知识库的增量编译稳定性。

---

## v3 新增机制

| 机制 | 命令 | 说明 |
|------|------|------|
| 增量编译 | `/wiki-compiler` | 哈希账本比对 → 只编译新增/修改文件 |
| 深夜做梦 | `/wiki-dream` | Gap/Stale/Bridge 结构感知采样，跨领域灵感连接 |
| 健康巡检 | `/wiki-status` | 死链/孤立页检测 + Stub 自动生成 |
| 知识检索 | `query_wiki.py` | BM25 全文检索（Agent 自用 / 用户调用） |
| 标签提取 | `tag_extractor.py` | 自动打标签（--apply-tags） |

---

## 演进意义

经过裁判隔离与分治降维处理后，系统完成了从"一段脚本"到"个人科研级副驾驶引擎"的蜕变：

1. **绝对的学术严谨性**：一字一句皆有出处，摆脱黑盒幻觉阴影。
2. **无上限的知识拓展**：Map-Reduce 让第二大脑能吞吐 50 乃至 500 篇文献流。
3. **闭环防腐**：通过精确的警告靶点标记知识盲区，形成持续生长的自适应飞轮。
4. **可检索的知识底座**：三层索引让"只记不读"的囤积变为"可扫描、可下钻、可综合"的活知识库。

---

> 长期路线图（矛盾检测 / 知识图谱持久化 / 主动研究助手 / 知识蒸馏微调）见 `ROADMAP.md`。
